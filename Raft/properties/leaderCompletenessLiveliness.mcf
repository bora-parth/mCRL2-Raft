[true*][clientCommand][true*] <true*> exists currentCommitIndex, nextCommitIndex, term1, index: Nat, log1: LogType . val(index > currentCommitIndex && index <= nextCommitIndex) && <advanceCommitIndex(currentCommitIndex, nextCommitIndex, term1, log1)> <true*> exists term2: Nat, log2: LogType . val(term2 > term1) && <exposeLogLeader(term2, log2)> true
