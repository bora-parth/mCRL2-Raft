mu X . (([true] X) || mu Y. ((exists id1, term1, commitIndex1: Nat, log1: LogType . val( id1 < 3 && term1 < 2 && commitIndex1 < 3) && <exposeLog(id1, term1, commitIndex1, log1)> <true*> exists id2, term2, index, commitIndex2: Nat, log2: LogType . val( id2 < 3 && term2 < 2 && commitIndex2 < 3) && <exposeLog(id2, term2, commitIndex2, log2)> (val(id1 != id2 && # log2 > 0 && log1 . index == log2 . index && # log1 > 0)))))
