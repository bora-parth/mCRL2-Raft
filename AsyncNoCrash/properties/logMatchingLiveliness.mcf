<true*> exists id1, term1, commitIndex1: Nat, log1: LogType . val(#log1 > 0) => <exposeLog(id1, term1, commitIndex1, log1)> <true*> exists id2, term2, index, commitIndex2: Nat, log2: LogType . val(id1 != id2 && # log2 > 0 && log1 . index == log2 . index) => <exposeLog(id2, term2, commitIndex2, log2)> true